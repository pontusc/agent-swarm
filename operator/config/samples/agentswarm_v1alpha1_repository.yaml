apiVersion: agentswarm.dev/v1alpha1
kind: Repository
metadata:
  labels:
    app.kubernetes.io/name: agent-swarm
    app.kubernetes.io/managed-by: kustomize
  name: repository-sample
spec:
  # TODO(user): Add fields here
